apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "usopp-apps.fullname" . }}-test-connection"
  labels:
    {{- include "usopp-apps.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "usopp-apps.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
